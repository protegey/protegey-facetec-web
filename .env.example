VITE_API_BASE=/api/v1
VITE_FACE_TEC_DEVICE_KEY=dlrL00OosNJyky981KCeSVtVW63vPvtM
# For a standalone deployment (e.g. Vercel), VITE_API_BASE should be the full
# protegey-backend URL instead of a relative path, e.g.
# VITE_API_BASE=https://api.protegey.app/api/v1 — it can also be overridden
# per-launch via the `apiBase` query param (see src/App.tsx), which is what
# protegey-partner-web actually sends when redirecting a partner here.
#
# Optional fallback for the postMessage target origin, used only in the
# fallback case where this app is opened inside an <iframe> rather than
# reached by a normal redirect (the primary launch mode). Usually passed
# per-launch via the `parentOrigin` query param instead; see
# src/services/embedBridge.ts.
# VITE_PARENT_ORIGIN=https://partner.protegey.app
